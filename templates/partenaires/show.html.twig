{% extends 'base.html.twig' %}

{% block title %}Partenaires{% endblock %}

{% block body %}
    <h1>Partenaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdPartenaire</th>
                <td>{{ partenaire.idPartenaire }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ partenaire.nom }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ partenaire.photo }}</td>
            </tr>
            <tr>
                <th>Site</th>
                <td>{{ partenaire.site }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ partenaire.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partenaires_index') }}">back to list</a>

    <a href="{{ path('partenaires_edit', {'idPartenaire': partenaire.idPartenaire}) }}">edit</a>

    {{ include('partenaires/_delete_form.html.twig') }}
{% endblock %}
