<form method="post" action="{{ path('partenaires_delete', {'idPartenaire': partenaire.idPartenaire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partenaire.idPartenaire) }}">
    <button class="btn">Delete</button>
</form>
