<form method="post" action="{{ path('panier_controller_delete', {'idPanier': tab.idPanier}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tab.idPanier) }}">
    <button class="btn btn-danger">Delete</button>
</form>
