{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-lg btn-primary btn-block" formnovalidate>{{ button_label|default('Sign Up') }}</button>
{{ form_end(form) }}
