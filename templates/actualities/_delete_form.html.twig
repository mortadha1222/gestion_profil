<form method="post" action="{{ path('actualities_delete', {'idAct': actuality.idAct}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actuality.idAct) }}">
    <button class="btn">Delete</button>
</form>
